[pytest]
# Safety net: test_priority_pool-style task-pool deadlocks previously hung the
# whole suite forever (observed 21-day-old stuck pytest workers). The cap only
# fires for hung tests; legitimately slow parity tests stay well under it.
timeout = 600
timeout_method = thread
